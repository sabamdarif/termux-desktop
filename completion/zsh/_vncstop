#compdef vncstop

_vncstop() {
    local -a opts
    opts=(
        "--force:Force stop VNC server"
        "-f:Force stop VNC server"
        "--help:Show help message"
        "-h:Show help message"
    )

    _describe 'VNC Stop Options' opts
}

_vncstop "$@"